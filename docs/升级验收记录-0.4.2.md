# 0.4.2 正文回读与 SMB 分发验收

日期：2026-09-20。关联 Mac 工作 `work-5d168143cfe6400eb99300db2db5ae91`。

## 变更

- 固定服务 URL 支持分页查询参数。请求参数仍按配置验证并编码；保留原主机、协议、内嵌凭据、fragment 和重定向限制。Hub 基础地址的验证规则不变。
- Windows 生命周期测试在资源锁释放后继续等待确切设备进程退出，再清理日志。等待句柄在终止父进程前取得并最终关闭；保留超时失败，不跳过 Windows 测试。
- 原 Mac 配置为 MyWeb 增加 `blog-content`、`blog-content-next` 两个正文读取服务，沿用原 Notion 引用及博客数据库归属检查。没有修改文章数据。
- 原统一分发配置登记 Mac SMB 和小米 SMB 两个位置。小米挂载位于 `~/Library/Application Support/AgentBoard/mounts/xiaomi-nvme-707f`，与源码目录分开，避免项目复制测试遍历整个共享盘。

## 实测依据

| 项目 | 结果与依据 |
| --- | --- |
| 完整 Mac 回归 | 77 项，76 通过、1 项既有跳过，12.623 秒；新增真实 HTTP 分页请求、游标编码、非法 URL 拒绝检查 |
| 静态检查 | `git diff --check`、`node --check board_network/static/app.js` 均退出 0 |
| 服务重启 | 06:57:17 UTC 启动唯一 0.4.2 service PID 70507、device PID 70509；`/health` 返回 0.4.2，doctor 无警告、无连接冲突 |
| 查询已有博客 | `operation-4e1f4fe79e504f4299224d5ca0ef1684`，HTTP 200；`mac-local` 确认取回 |
| 读取文章属性 | `operation-2957a569e46748efb3ffa1277b4974e4`，HTTP 200；确认原文章已发布，没有修改属性 |
| 正文顶层回读 | `operation-dcbc6b75d9fd40379d0b6a3f486f8979`，HTTP 200，100 个顶层块，`has_more=false`，6 个块有子内容；Mac 于 06:58:02 UTC 确认取回。没有据此声称 6 个嵌套部分已完整读取 |
| 小米实际传输 | Mac 将首轮 0.4.1 的 190620 字节归档复制到 `\\192.168.1.237\XiaoMi-NVMe(707F)\AgentBoard\releases\0.4.1-fe4c336f6e09`，回读 SHA256 `e82b53c4ec9231cd3fcbe7cf163f810ea2be582f03c240d782f1a6ec2072ec3d`，与 Mac SMB 和原清单一致 |
| 配置保留 | 修改前备份并逐字节核对，变更后核对：除新增正文服务与 SMB 分发项外，资源、配对、凭据引用和其他配置均未变。没有删除、移动或重建知识库 |

正文探测先在 0.4.1 发现查询字符串被拒绝，失败操作 `operation-e67c9c9f38ea42c385c84c781d22f7e9` 保留。修复并重启后发起新的只读请求，未把旧失败改成成功。

本轮曾因将 SMB 临时挂入源码树，导致旧安装器测试复制共享内容而超时；已停止该测试，将挂载改到系统运行目录，完整重跑得到上述 77 项结果。未修改共享盘原有文件。

本机证据：`output/validation/myweb-content-readback-20260920.json`、`xiaomi-smb-20260920.json`；配置备份包含 `.runtime/config.json.before-xiaomi-93854da8`、`.runtime/config.json.before-resource-61fdce40`，均不进入源码包。

## 尚未完成

1. Windows 当前仍运行 0.4.1，需要拉取 0.4.2 后原生复测生命周期错误；Mac 通过不能代替 Windows 结果。
2. Windows 当前聊天实际绑定并调用设备级 MCP 尚未验证，当前远程终端入口仍不可用。设备级资源通道已双向验证，详见 [0.4.1 记录](升级验收记录-0.4.1.md)。
3. 用户选择以另一个当前 Windows 工程验收，具体工程及博客目标待提供。尚未创建新草稿，也没有正式发布文章。
4. 真实 Notion `blog-content-next` 尚未遇到有后续页的样本；分页 URL/参数已在真实本地 HTTP 回归测试中验证。嵌套正文访问需继续沿文章归属边界实现与验证。
5. Windows 从小米共享下载新包，以及 Mac 重启后的共享重新挂载，尚未实机验证。

下一步按[最后一公里验收](最后一公里验收.md)执行。0.4.2 归档经既有发布器写入两个 SMB，最终提交与文件 SHA256 以各共享的 `latest.json` 和版本目录清单为准。
