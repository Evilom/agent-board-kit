# 0.4 设备生态升级验收

验证日期：2026-09-19。当前结论：Mac 已部署，MyWeb 实际资源操作通过；Windows 的 0.3 心跳已恢复，0.4 跨机实机验收仍未通过，不能宣布全部设备已经打通。

关联基础设施任务：`work-5d168143cfe6400eb99300db2db5ae91`。没有认领、更新或代验收 Windows 所有的任务，也没有执行历史博客发布任务。

## 交付内容

- 设备级资源目录：工程位置、设备在线状态、访问范围、上下文、固定命令、服务与凭据引用。
- 持久操作队列：读取、条件写入、知识搜索、命令、服务调用和分块传输。目标设备不需要打开聊天会话。
- 执行账本：请求编号去重、原子认领、权限复核、SHA256 冲突检查、回执恢复；不确定的操作不会自动重做。
- 统一配置引用：继续使用原知识原文、现有索引及设备上的凭据位置，不复制密钥或重建知识库。
- 用户登录自启服务、独立的 5 工具 MCP 入口，以及默认展示工程目录的网页。

## 逐项验收

| 标准 | 结论 | 实际依据 |
| --- | --- | --- |
| 发现跨工程资源与能力 | Mac 通过；跨机待验 | 实际 Hub 返回 Agent Board、MyWeb 两项资源及在线状态；独立 MCP 进程搜索“博客”找到 MyWeb，未新增聊天 Agent |
| 无需对端聊天会话即可执行 | Mac 通过 | 同一真实队列完成 MyWeb 上下文、临时文件写入/回读、图片检查及 Notion 查询；操作编号见下表 |
| 权限、路径、写入冲突、回执恢复 | 自动测试通过 | 两套认证设备身份测试并发认领、权限撤回、路径与链接边界、凭据文件搜索过滤、冲突写入、丢失回执后只补交结果、超时与停止命令；这不是 Windows 原生测试 |
| 上下文来源与凭据引用 | Mac 通过 | 上下文带资源 URI 与原文 SHA256；Notion 数据库结构实测 HTTP 200；凭据文件字节未改变；注入凭据的命令/服务脱敏测试通过 |
| 旧数据完整、测试与 Mac 闭环 | 通过 | 69 项测试中 68 项通过、1 项不适用而跳过；数据库和备份 integrity_check=ok；既有记录主键无缺失，原配置所有字段保持一致；服务重启后再次成功读取 MyWeb |
| Windows 原生发现、执行并取回结果 | **未验证** | 初次验收时离线，14:31 UTC 已恢复 windows-main 的 0.3 心跳；桌面连接仍报告 tunnel_client_not_seen。未在真实 Windows 安装 0.4 或运行验收 |

## Mac 实际操作回执

MyWeb 资源：`resource-fa9da9afbfb5fdecc5cfa7ef07c4cffe`。以下请求者与执行设备均为 `mac-local`。

| 操作 | 操作编号 | 返回证据 |
| --- | --- | --- |
| 读取工程上下文 | `operation-9ac42296ee604268a30fdc0df1472b1c` | succeeded；AGENTS.md、README.md、package.json 原文及 SHA256 |
| 写入临时验收文件 | `operation-a1dc91e0db5b436bad6dc27556b17693` | succeeded；48 字节；SHA256 见下文 |
| 回读临时文件 | `operation-54fd5aac5c864fb288874689aa6b7f75` | succeeded；与写入的 SHA256 相同；临时文件随后清理 |
| 调用图片检查 | `operation-8224afde6a7a4c6cb67a3beba99b9ed3` | succeeded；退出码 0 |
| 查询 Notion 博客结构 | `operation-c8b54cd7f78a4057b97ce530665a896f` | succeeded；HTTP 200；存在 Name、Date、Excerpt、Published、ReadTime、Tags 字段 |
| 网页发起图片检查 | `operation-de68ec0225c446618e4c95d73a6d68d1` | succeeded；退出码 0 |
| 服务重启后读取上下文 | `operation-6bb46b56014244ad9e0ab554aec6f013` | succeeded；返回 3 份上下文 |

临时文件的写入与回读 SHA256：`1d469027a65cc085ed15347faa45894e4eafc6deeb1cfb462189e2a4efa8a858`。

真实共享知识查询 Notion 返回 8 条结果，均带 SHA256，4 项来源状态为 available。过程中修复了已有知识源返回命中时缺失 digest 导入的问题，并增加跨项目授权下既有来源去重的回归测试。

机器可读回执保存在本机 `output/validation/mac-ecosystem-0.4.json`、`output/validation/data-integrity-0.4.json`；运行文件不随便携源码包分发。

## 配置、数据与重启

- 原配置备份：`.runtime/config.json.before-0.4-58b0dc8f`。
- 原数据库备份：`.runtime/board.before-0.4-58b0dc8f.sqlite3`。
- 工程登记前备份：`.runtime/config.json.before-ecosystem-11857348`。
- 资源接入新增 resources、credential_refs，随后 SMB 发布新增 distribution；既有知识源、项目、设备配对及其他原字段与升级前逐项相同。
- 旧数据库的 tasks 10 条、collab_records 41 条、collab_events 63 条、collab_requests 29 条及 audit 56 条，全部原主键保留。原 tasks/events/requests/audit 整行内容也逐条保留。
- Mac 用户 LaunchAgent 已启动，并经过停止原服务、自动重新启动、重新执行操作的实测。没有模拟一次真实注销/登录或系统重启。
- Codex 全局配置新增 agent_ecosystem。与 `.codex/config.toml.before-agent-ecosystem-f199aacd` 比较，其他设置保持一致。独立 MCP 子进程可返回 5 个工具和真实目录；当前已经启动的会话仍需 MCP 重载才能获得新的工具绑定。
- 网页短期登录会话在服务重启后失效；重新从本机 `network ... open` 打开即可使用原有配对信息换取登录，不需要复制令牌。

## 自动测试与界面

```sh
python3 -m unittest discover -q
node --check board_network/static/app.js
git diff --check
```

结果：Ran 69 tests，OK (skipped=1)；后两项退出码 0。唯一跳过项为 test_embedded_project_schema_and_compatibility_entry_stay_in_sync，原因是当前为独立工具仓库，没有嵌入式宿主工程。

真实浏览器检查了工程目录、上下文展开、命令选择与提交、知识查询。检查 1200 像素桌面宽度与 390 像素手机窗口；手机页面实际内容宽度与滚动宽度均为 375 像素，没有页面横向溢出。上下文原文按文件展示，来源和 SHA256 可展开。截图保存在本机 `output/playwright/engineering-context.png`、`engineering-mobile.png`。

## 尚未完成的验收

1. Windows 原生升级、登录自启、MCP 加载及 Windows → Mac MyWeb 的真实操作回执。
2. Linux 用户服务仅生成并检查定义，未进行 Linux 实机安装。
3. 分块传输和中断恢复已通过自动测试，未在 Windows 与 Mac 之间传输实际发布包。
4. Notion 创建、修改、追加接口的范围限制通过本机模拟服务测试；真实 Notion 仅查询数据库结构。没有创建文章、公开发布文章或执行生产部署。

Windows 恢复连接后按[设备生态使用指南](设备生态使用指南.md)升级现有客户端。以 Windows 自己的真实身份调用 board_resources 查找 MyWeb，再依次读取 context、执行 check-images、取回同一操作的成功回执。只有保存这些实机依据后，第六项才能改为通过。


## 本轮追加：推送、SMB 与深色主题

用户明确要求将源码提交并推送，供 Windows 从 Git 拉取；升级包存入 SMB，页面采用 ChatGPT 风格深色主题。本次授权覆盖源码发布，不改变 Windows 任务归属。

- 源码分发使用既有 origin/main；Windows 更新脚本已经加入 test_ecosystem 测试。
- 发布目标保存在原配置的 distribution.smb_shares。Mac SMB 根目录为 `\\192.168.1.199\data\AgentBoard\releases`，对应 `/Volumes/superdisk/data/AgentBoard/releases`。
- `scripts/package_network.py --publish` 只发布已提交源码，生成按版本和提交号区分的目录、manifest.json 和 latest.json；原版本不覆盖。具体发布提交号和 SHA256 以共享中的 manifest 为准。
- 配置变更前已验证备份 `.runtime/config.json.before-smb-distribution-3e718f0f`，其他原配置保持一致。
- 发布器新增两项测试，覆盖副本 SHA256、重复发布、旧版本保留、内容冲突拒绝、未挂载目录拒绝。完整测试为 69 项，68 通过、1 跳过。
- 深色主题覆盖主页面、侧栏、表单、弹窗、代码块、状态和错误提示。真实浏览器检查桌面与手机宽度；截图为 output/playwright/dark-desktop.png、dark-context.png、dark-mobile.png。
- Mac SMB 的 data 共享配置、挂载盘和 TCP 445 监听均已核对；继续使用 Windows 既有 SMB 登录。当前 Mac 的免密 SMB 登录被拒绝，没有据此声称 Windows 已读取新包。
- 小米路由器的 SMB 未挂载，192.168.1.1 的免密连接被拒绝；它的共享名称与有效挂载位置仍待确认，没有写入或更改路由器配置。
