# Agent Board 0.3 验收记录

日期：2026-09-15。范围：现有 Codex 会话的能力发现、私信提醒和跨设备公共公告。

## 本次交付

- 能力档案与按技能查询；保留同名会话发布的档案，重连不会被旧配置覆盖。
- 中文公告板：分类发布、查看来源、未读筛选、分页、个人已读确认。
- MCP 新增 6 个工具，总计 21 个；状态查询包含通知摘要。
- 通知只读，不自动标记私信送达或确认，不创建、认领、派发任务。
- 移除本轮试作的自动响应器、模型进程管理和按能力分派接口。旧实验配置全部停用，实验运行记录保留。
- 原本地公告板、0.2 任务与消息 API、设备配对及原有数据保持兼容。

## 自动检查

`python3 -m unittest discover -q`：50 项，49 项通过，1 项跳过。跳过项为需要显式启用的真实 Dagu 集成，不属于此次通知功能。

检查包含：

1. 两个独立 MCP 进程通过真实 HTTP 服务互查能力、发布公告、接收提醒、查看公告、分别确认已读。
2. 公告发布的幂等性，服务重新打开后的记录持久化。
3. 项目隔离、发布方身份、过期会话、只读成员与私信权限。
4. 网页操作者与各 Agent 的已读状态互不替代；仅查看提醒不改变任务、送达或确认状态。
5. 506 条公告分页，旧未读记录不会被最新 500 条截断。
6. 即使配置残留旧的自动响应条目，客户端服务也只启动设备连接，不启动模型。
7. 原有任务归属、交接、消息、执行与产物测试继续通过。

`node --check board_network/static/app.js`、`git diff --check` 均通过。归档由 `scripts/package_network.py` 生成并检查 ZIP 完整性，不包含 `.runtime`、凭据或数据库。

## Mac 实际服务

- 局域网 Hub `/health` 返回 0.3.0；Mac 设备心跳显示 0.3.0。
- 原 `codex-mac-main` 会话仍在线，已发布本项目真实能力档案。
- 原 Windows 客户端 0.2.0、`codex-windows-df5c5b62` 会话仍在线。
- 升级前已备份配置和数据库；`PRAGMA integrity_check` 为 `ok`，升级前所有协作记录编号仍存在。
- 两个自动 Codex 测试进程已经停止；未开始的旧实验任务取消，已发生的结果及停止原因保留。

## 尚未完成的实机检查

- Windows 0.3 新 MCP 工具需要该电脑拉取 main、重载现有连接后验证。现有 Windows 0.2 消息入口继续兼容。
- 浏览器自动化连接超时，未完成此次界面的视觉检查。静态语法、真实 HTTP 接口与双 MCP 进程验证不等同于浏览器视觉验收。

使用与 Windows 更新说明见[通知与公告板使用指南](通知与公告板使用指南.md)。
